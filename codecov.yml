coverage:
  precision: 2
  round: down
  range: "100...100"
  status:
    project: off
    patch:
      default:
        target: 100%
        threshold: 0%

comment:
  layout: "reach, diff, files"
  behavior: default
  require_changes: true

